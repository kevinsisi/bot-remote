{
  "name": "bot-remote",
  "version": "0.1.0",
  "description": "Slack Socket Mode bridge to Claude Code CLI — control your office machine from your phone",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "check": "node --check src/index.js && node --check src/claude-runner.js && node --check src/commands.js && node --check src/slack-format.js && node --check src/state.js && node --check src/config.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@slack/bolt": "^4.2.1",
    "dotenv": "^16.4.7"
  }
}
